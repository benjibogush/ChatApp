const panel = document.getElementById('adminModal')

function adminPanelOpen() {
  panel.style.display = "block"
}

function closeAdminModal() {
  panel.style.display = "none"
}
// wait, shouldn't it be fixed instead of block? no need kk. also admin panel btn wont work try now pog. also why is my close modal button not positiponed
